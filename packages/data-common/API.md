## API Report File for "@aws-blocks/data-common"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Kysely } from 'kysely';

// @public
export function createKyselyAdapter<T>(db: {
    getEngine(): DatabaseEngine | Promise<DatabaseEngine>;
}): Kysely<T>;

// @public
export class DatabaseBase {
    constructor(engine: DatabaseEngine);
    // (undocumented)
    protected engine: DatabaseEngine;
    execute(query: SqlQuery): Promise<{
        rowCount: number;
    }>;
    getEngine(): DatabaseEngine;
    query<T>(query: SqlQuery): Promise<T[]>;
    queryOne<T>(query: SqlQuery): Promise<T | null>;
    transaction<T>(fn: (tx: Transaction) => Promise<T>): Promise<T>;
}

// @public
export interface DatabaseEngine {
    beginTransaction(): Promise<TransactionHandle>;
    commitTransaction(handle: TransactionHandle): Promise<void>;
    destroy(): Promise<void>;
    execute(sql: string, params?: unknown[]): Promise<{
        rowCount: number;
    }>;
    executeInTransaction(handle: TransactionHandle, sql: string, params?: unknown[]): Promise<{
        rowCount: number;
    }>;
    query<T>(sql: string, params?: unknown[]): Promise<T[]>;
    queryInTransaction<T>(handle: TransactionHandle, sql: string, params?: unknown[]): Promise<T[]>;
    rollbackTransaction(handle: TransactionHandle): Promise<void>;
}

// @public
export const DOLLAR_QUOTE_TAG_RE: RegExp;

// @public
export const loadMigrationsFromDir: (dir: string) => Promise<Record<string, string>>;

// @public
export const runMigrations: (engine: DatabaseEngine, migrations: Record<string, string>) => Promise<string[]>;

// @public
export const splitStatements: (sql: string) => string[];

// @public
export function sql(strings: TemplateStringsArray, ...values: unknown[]): SqlQuery;

// @public
export interface SqlQuery {
    // (undocumented)
    readonly [SQL_BRAND]: true;
    // (undocumented)
    readonly params: readonly unknown[];
    // (undocumented)
    readonly sql: string;
}

// @public
export interface Transaction {
    execute(query: SqlQuery): Promise<{
        rowCount: number;
    }>;
    query<T>(query: SqlQuery): Promise<T[]>;
    queryOne<T>(query: SqlQuery): Promise<T | null>;
}

// @public
export type TransactionHandle = unknown;

// Warning: (ae-internal-missing-underscore) The name "unwrapQuery" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal
export function unwrapQuery(query: SqlQuery): {
    sql: string;
    params: unknown[];
};

// (No @packageDocumentation comment for this package)

```
