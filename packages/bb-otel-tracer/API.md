## API Report File for "@aws-blocks/bb-otel-tracer"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Context } from '@opentelemetry/api';
import { Link } from '@opentelemetry/api';
import { Scope } from '@aws-blocks/core';
import type { ScopeParent } from '@aws-blocks/core';
import { SpanKind } from '@opentelemetry/api';
import { Tracer } from '@opentelemetry/api';

// @public
export type AnnotationValue = string | number | boolean;

export { Context }

export { Link }

// @public
export class OtelTracer extends Scope {
    constructor(scope: ScopeParent, id: string, options?: OtelTracerOptions);
    addAnnotation(key: string, value: AnnotationValue): void;
    addEvent(name: string, attributes?: Record<string, AnnotationValue>): void;
    addMetadata(key: string, value: unknown): void;
    extract(carrier: Record<string, string>): Context;
    getTraceId(): string | null;
    inject(carrier: Record<string, string>): void;
    get rawTracer(): Tracer;
    startSegment<T>(name: string, fn: (segment: Segment) => Promise<T>, options?: StartSegmentOptions): Promise<T>;
}

// @public
export interface OtelTracerOptions {
    enabled?: boolean;
    serviceName?: string;
    serviceNamespace?: string;
    serviceVersion?: string;
}

// @public
export interface Segment {
    addAnnotation(key: string, value: AnnotationValue): void;
    addError(error: Error): void;
    addEvent(name: string, attributes?: Record<string, AnnotationValue>): void;
    addMetadata(key: string, value: unknown): void;
    setHttpStatus(statusCode: number): void;
}

export { SpanKind }

// @public
export interface StartSegmentOptions {
    attributes?: Record<string, AnnotationValue>;
    kind?: SpanKind;
    links?: Link[];
}

export { Tracer }

// (No @packageDocumentation comment for this package)

```
